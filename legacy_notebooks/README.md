# Legacy notebooks

These notebooks are development notebooks preserved for transparency. They are not the canonical reproduction path for the manuscript.

For reproducible execution, use the scripts in `src/` and the notebooks in `notebooks/`.
